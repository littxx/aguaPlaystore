import React from 'react';
import {NavigationContainer} from '@react-navigation/native';
import PConfig from './Pconfig.routes';

function Rotas() {
  return (
    <NavigationContainer>
      <PConfig />
    </NavigationContainer>
  );
}

export default Rotas;
